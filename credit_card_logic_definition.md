# てどりクエスト：クレジットカードロジック＆データベース定義書

## 1. クレジットカードの評価・クエスト発生ロジック

ユーザーの保有カード状況と決済額から、「低効率なメインカード」や「ムダな年会費（休眠負債）」などの魔道障壁（クエスト）を自動検出する。

### 月間・年間カード決済額の自動推定
ユーザーの入力の手間を省くため、手取りと固定費から以下の式で推定する。
* `推定月間決済額 = 現状の固定費合計 ＋ Math.floor((月額手取り － 現状の固定費合計) * 0.6)`
* `推定年間決済額 = 推定月間決済額 × 12`

### クエスト判定パターン

#### パターンA：【低効率装備の換装】
* **発生条件:** メインカードの属性が `category === 'standard'`（基本還元率0.5%程度で特化強みなし）の場合。
* **節約可能額:** `Math.floor(推定年間決済額 × 0.01)` （※還元率1.5%相当の理想装備に乗り換えた場合の差分1.0%として計算）
* **考え方:** 基本還元率の低いカードから、高還元カードへの乗り換えを促す。

#### パターンB：【呪いの装備の解除（休眠カードの断捨離）】
* **発生条件:** 使っていない「その他保有カード」に、年会費が発生する有料カードが存在する場合。
* **節約可能額:** `該当カードの合計年会費`
* **クエスト提示メッセージ（王道RPG風）:**
  > **【クエスト発生】呪いの装備を外してHPの漏れを塞げ！**
  > あなたの装備袋から、不穏な気配がします。現在保有している「〇〇カード」は、十分な活躍をしていないにもかかわらず、毎年〇〇円の『年会費』というスリップダメージをあなたのHP（手取り）に与え続けています。
  > なんとなく持っているだけの「呪いの装備」は、思い切って手放すのが冒険の鉄則です。今すぐカード会社のマイページや窓口に向かい、装備を解呪（解約・ダウングレード）して、奪われていた手取りを取り戻しましょう！

#### パターンC：【100万円未達の罠】
* **発生条件:** 100万円修行カード（`is1M_Challenge === true`）を保有し、`推定年間決済額 < 1000000` の場合。
* **節約可能額:** `発生する年会費 ＋ (推定年間決済額 × 0.005)`
* **考え方:** 100万円決済に届いていないため、年会費が発生し還元率も0.5%に留まっている状態を警告。

#### パターンD：【コスト割れ特化装備】
* **発生条件:** ホテル系など損益分岐点がある特化カード（`breakEvenAmount`設定あり）を保有し、`推定年間決済額 < breakEvenAmount` の場合（※聖域カードを除く）。
* **節約可能額:** `該当カードの年会費`
* **考え方:** 無料宿泊特典などのメリットを享受できる決済ラインに届いておらず、年会費が隠れ負債になっている状態を警告。

---

## 2. クレジットカードのカテゴリと特殊ロジック分類基準

各カードデータには、プレイスタイルや目的を許容するため以下の属性・フラグを付与する。

* **`category`（基本属性）**
  * `'standard'`（一般・低還元）：基本還元率0.5%前後。メイン設定時にパターンA発生。
  * `'high_reward'`（高還元）：基本還元率1.0%以上。現状維持OK。
  * `'specialty'`（特典・目的特化）：特定店舗や目的で威力を発揮。現状維持OK（条件あり）。
* **`is1M_Challenge`（100万円修行フラグ）**
  * `true`の場合、年間100万円決済で年会費無料化＋還元率1.5%相当に跳ね上がる特殊判定を行う。
* **`breakEvenAmount`（損益分岐点）**
  * 年会費の元が取れる、または無料宿泊等の強力な特典が付与されるボーダー決済額。
* **`pointValueMultiplier`（価値乗数）**
  * マイル等、1pt=1円以上の価値がある場合の補正値（ANAカード等は2.0と設定し、実質還元率を2倍で評価）。
* **`isStatusCard`（聖域フラグ）**
  * SFCやJGCなど、ステータス維持目的のカード。`true`の場合、無条件でクエスト判定から除外する。

---

## 3. クレジットカード初期データベース（JavaScript配列イメージ）

```javascript
const eliteCreditCards = [
  // 1. 100万円修行カード（決済額に応じて還元率と年会費が変動）
  { 
    id: "smcc_gold_nl", 
    name: "三井住友カード ゴールド（NL）", 
    basePointRate: 0.5, 
    annualFee: 5500, 
    category: "specialty", 
    is1M_Challenge: true,
    breakEvenAmount: 1000000 
  },
  { 
    id: "epos_gold", 
    name: "エポスゴールドカード", 
    basePointRate: 0.5, 
    annualFee: 5000,
    category: "specialty",
    is1M_Challenge: true,
    breakEvenAmount: 1000000 
  },
  
  // 2. マイル系カード（価値乗数で実質還元率を補正）
  { 
    id: "ana_visa_suica", 
    name: "ANA VISA Suicaカード", 
    basePointRate: 0.5, 
    pointValueMultiplier: 2.0, // 実質1.0%とみなす
    annualFee: 2200, 
    category: "specialty",
    isStatusCard: false
  },
  
  // 3. ホテル系カード（特定の決済額で元が取れる）
  { 
    id: "hilton_amex", 
    name: "ヒルトン・オナーズ アメリカン・エキスプレス・カード", 
    basePointRate: 1.0, 
    pointValueMultiplier: 1.0, 
    annualFee: 16500, 
    category: "specialty",
    breakEvenAmount: 1500000 // 無料宿泊特典の条件
  },
  
  // 4. ステータス・聖域カード（判定から完全に除外）
  { 
    id: "ana_sfc_gold", 
    name: "ANA SFC ゴールドカード", 
    basePointRate: 1.0, 
    pointValueMultiplier: 2.0, // マイル還元率等に応じた補正
    annualFee: 16500, 
    category: "specialty",
    isStatusCard: true // クエスト対象外
  },
  
  // 5. 一般・低還元カード（換装対象）
  { 
    id: "mufg_viaso", 
    name: "三菱UFJカード VIASOカード", 
    basePointRate: 0.5, 
    pointValueMultiplier: 1.0,
    annualFee: 0, 
    category: "standard",
    isStatusCard: false
  },
  
  // 6. 高還元カード（合格ライン）
  { 
    id: "rakuten", 
    name: "楽天カード", 
    basePointRate: 1.0, 
    pointValueMultiplier: 1.0,
    annualFee: 0, 
    category: "high_reward",
    isStatusCard: false
  }
];
```
