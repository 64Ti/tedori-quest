---
title: 手取りクエスト UIデザイン実装仕様書（王道クラシックRPGスタイル編）
---

# 手取りクエスト UIデザイン実装仕様書

このドキュメントは、本作「てどりクエスト」を、誰もが親しみを持つ「王道クラシックRPG（8bit/16bit風）」の世界観に没入させるためのUIデザイン実装ルールを定義したものです。

## 1. 基本設計方針（クラシックRPGの徹底）

1.  **コマンドウィンドウの再現**: すべてのコンテンツブロック、入力フォーム、ポップアップを「黒背景＋太い白枠（二重線または単線）＋角丸」のクラシックなメッセージウィンドウ風に統一する。
2.  **ドット絵風フォントの採用**: システムフォントを廃止し、ピクセルアート風のフォントを採用してレトロゲームの空気感を演出する。
3.  **カーソル（▶）によるナビゲーション**: ボタンのホバー時や選択中項目には、左側に「▶」などのカーソルアイコンを表示させ、RPGのコマンド選択の操作感を再現する。

## 2. カラーパレット（CSS Variables）

配色はレトロRPGのシステム画面を再現する以下のカラーに限定する。

| 用途 | CSS変数 | カラーコード | 適用箇所・備考 |
| :--- | :--- | :--- | :--- |
| **ウィンドウ背景** | `--window-bg` | `#000000` | コンテナ、ボタンの背景（完全な黒） |
| **ウィンドウ枠線** | `--window-border`| `#FFFFFF` | ウィンドウの枠線、区切り線 |
| **テキスト（基本）**| `--text-main` | `#FFFFFF` | 通常のテキスト（白） |
| **強調・ゴールド** | `--text-gold` | `#F8D820` | レベル、称号、金額などのポジティブな強調 |
| **警告・ダメージ** | `--text-danger` | `#E60012` | 警告、エラーメッセージ、マイナス要素 |
| **ゲージ（EXP）** | `--bar-exp` | `#2080D0` | 経験値・進捗バー（青系） |
| **画面全体背景** | `--bg-body` | `#111111` | ウィンドウの外側の背景色（真っ黒より少し浮かす）|

## 3. タイポグラフィ（フォント）

*   **指定フォント**: Google Fontsの `'DotGothic16', sans-serif` を最優先で読み込み、全体に適用する。
*   **行間と字間**: 読みやすさを確保しつつ、レトロゲーム特有の「文字が詰まっている感」を出すため、`line-height: 1.5;`、`letter-spacing: 2px;` 程度に設定する。
*   **文字サイズ**: 
    *   H1/H2 (タイトル類): `24px`
    *   本文・ボタン: `16px`
    *   注記: `12px`

## 4. UIコンポーネント実装詳細

### 4.1 ウィンドウ（`.dq-window` または各コンテナ）
*   **スタイル**: 
    ```css
    background-color: var(--window-bg);
    border: 4px solid var(--window-border);
    border-radius: 8px; /* わずかな角丸 */
    padding: 16px;
    ```
*   （※可能であれば `border-style: double; border-width: 6px;` にすることで、より王道の二重枠ウィンドウを再現）

### 4.2 ボタン（コマンド選択）
*   **通常時**: 背景は `--window-bg`、文字は `--text-main`。枠線は `--window-border`（2px程度）。
*   **ホバー時/アクティブ時**: 
    *   背景色を反転させない（黒のまま）。
    *   CSSの `::before` 疑似要素を使用して、テキストの左側に **「▶ 」**（カーソル）を出現させる。
    *   例: `▶ ぼうけんをはじめる`

### 4.3 ステータスバー（EXPゲージ）
*   枠線は白（`--window-border`）、背景は黒（`--window-bg`）、伸びる中身のバーを青（`--bar-exp`）とする。
*   視認性を高めるため、バーの太さは `16px`〜`20px` と厚めにとる。

### 4.4 アラート・メッセージボックス
*   画面下部や中央に固定表示される、RPGの「会話ウィンドウ」風にレイアウトする。
*   文字が1文字ずつ表示されるタイピングアニメーション（CSS/JS）を入れるとより雰囲気が増すが、必須ではない。

### 4.5 フォーム（入力欄・セレクトボックス）
*   デフォルトのブラウザスタイル（白い入力欄）をリセットする。
*   背景色を黒、文字色を白、下線（`border-bottom`）のみを白にするか、黒背景＋細い白枠のドット風デザインにする。