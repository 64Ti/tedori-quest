# クエスト一覧（サブスクリプション・機能凍結中）

> ⚠️ **この機能は 2026-08-08 時点で一時凍結（非表示）中です。**
> 入力UI（STEP2の「🎬 サブスクリプション」カード）は `hidden` 属性で非表示にし、
> クエスト判定（`js/selectors.js` の `buildQuestList()`）からも除外しています。
> ロジック・マークアップ自体は将来の再開に備えて削除せず残してあります
> （`js/selectors.js` の該当行はコメントアウト、`index.html` は `hidden` 属性）。

- ソース: `js/calc.js`（`evaluateFixedCostQuests` の `subscriptions` カテゴリ）,
  `js/config.js`（`FIXED_COST_TARGETS.subscriptions`, `QUEST_CATALOG.cancelSubscription`,
  `SUBSCRIPTION_PLANS`）, `js/selectors.js`（`planSubscriptionTotal`, `subscriptionTotal`）
- 抽出日: 2026-08-08

## 発生条件

- 節約可能額 = `プラン選択分のサブスク月額合計（planSubscriptionTotal） − 1,000円（理想の目標値）`
- 節約可能額が **500円未満**の場合は足切りされ、一覧に表示されない（凍結解除後の挙動）
- 全国平均は ¥2,500
- **手動入力サブスク（自由入力枠・ジム等）はクエスト判定には使われない**。
  固定費合計（家計圧迫度・レベル計算）には算入されるが、「ムダの自動判定」の対象からは
  意図的に除外されている（本人の意思で契約している支出として扱うため）。

## クエスト内容

- **クエストID**: `cancelSubscription`
- **メインタイトル**: 【潜む吸血魔素の追放】使っていないサブスクを断ち切れ！
- **中立表記（シェア文面用）**: 未利用サブスク（動画・音楽配信等）の整理
- **詳細テキスト**:
  > 月に数回しか使っていない動画・音楽配信は、手取りをじわじわ奪う吸血魔素！カード明細から幽霊サブスクを発見し、今の自分に必要なものだけを残して契約を整理しよう！
- **根拠テキスト**:
  > 選択されたプランの月額合計（§6.2.12の価格データに基づく概算）。同じカテゴリで複数契約している場合や、Amazon プライムと Prime Video のように会費が重複している場合は特に確認の余地があります。
- **確認事項リスト（talkScript）**:
  1. 最後に利用した日はいつか（各サービスの視聴履歴で確認できます）
  2. 年額プランの場合、次回更新日と中途解約時の返金の可否
  3. App Store / Google Play 経由の課金か（その場合は各ストアから手続きが必要です）
  4. 無料体験中でないか。体験終了日はいつか
  5. 解約後、ダウンロード済みのコンテンツが視聴できなくなるか
  6. 通信キャリアのセット割やポイント還元の対象になっていないか
- **注意書き**: 年額プランの場合、解約時期により返金の扱いが異なります。
- **チェック時の動的テキスト**: 🎉 サブスクを解約した！

## 参考：入力UIのデータ構造（凍結中の入力対象）

`js/config.js` の `SUBSCRIPTION_PLANS` に、カテゴリ→サービス→プランの階層で価格データを保持している
（動画配信／音楽配信／電子書籍・オーディオ／クラウド・AI／ゲームの5カテゴリ、合計25サービス超）。
選択結果は `state.selections.subscriptionPlanIds`（プランIDの配列）にのみ保存し、
合計金額は `C.sumSubscriptions()` で都度算出する方式（金額そのものは保存しない）。
価格データ自体はこのMarkdownでは転記せず、`js/config.js` を正とする。
