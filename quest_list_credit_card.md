# クレジットカード機能一覧（機能凍結中）

> ⚠️ **この機能は 2026-08-08 時点で一時凍結（非表示）中です。**
> 入力UI（画面3の「💳 保有カード」カード）は `hidden` 属性で非表示にし、
> クエスト判定（`js/selectors.js` の `buildQuestList()`）からも除外しています。
> ロジック・マークアップ自体は将来の再開に備えて削除せず残してあります
> （`js/selectors.js` の該当行はコメントアウト、`index.html` は `hidden` 属性）。

- ソース: `js/creditCards.js`（カードDB）, `js/calc.js`（`evaluateCreditCardQuests`, `estimateCardSpend`）,
  `js/config.js`（`CARD_QUEST_TEXT`, `CARD_TARGET_RATE_PERCENT`, `CARD_SPEND_DISPOSABLE_RATIO`）
- 抽出日: 2026-08-08

---

## 1. カード初期データベース（`js/creditCards.js` `CREDIT_CARDS`）

| ID | カード名 | 基本還元率 | 年会費 | 属性(category) | 100万円修行 | 損益分岐点 | 価値乗数 | 聖域フラグ |
|---|---|---|---|---|---|---|---|---|
| `smcc_gold_nl` | 三井住友カード ゴールド（NL） | 0.5% | ¥5,500 | specialty | ✅ | ¥1,000,000 | — | — |
| `epos_gold` | エポスゴールドカード | 0.5% | ¥5,000 | specialty | ✅ | ¥1,000,000 | — | — |
| `ana_visa_suica` | ANA VISA Suicaカード | 0.5% | ¥2,200 | specialty | — | — | 2.0（実質1.0%扱い） | false |
| `hilton_amex` | ヒルトン・オナーズ アメックス | 1.0% | ¥16,500 | specialty | — | ¥1,500,000（無料宿泊特典の条件） | 1.0 | — |
| `ana_sfc_gold` | ANA SFC ゴールドカード | 1.0% | ¥16,500 | specialty | — | — | 2.0 | **true（判定対象外）** |
| `mufg_viaso` | 三菱UFJカード VIASOカード | 0.5% | ¥0 | **standard** | — | — | 1.0 | false |
| `rakuten` | 楽天カード | 1.0% | ¥0 | high_reward | — | — | 1.0 | false |

属性（`category`）の意味：

- `standard`：基本還元率0.5%前後・特化した強みなし → メインカードに設定するとパターンA発生
- `high_reward`：基本還元率1.0%以上 → 現状維持でよい
- `specialty`：特典・目的特化 → 現状維持でよい（条件付き）
- `isStatusCard: true`（ANA SFCゴールドのみ）→ 無条件でクエスト判定から除外される「聖域カード」

---

## 2. 推定決済額の算出（`estimateCardSpend`）

```
推定月間決済額 = 現状の固定費合計 + floor((月額手取り − 現状の固定費合計) × 0.6)
推定年間決済額 = 推定月間決済額 × 12
```

`CARD_SPEND_DISPOSABLE_RATIO = 0.6`（可処分所得のうち60%をカード決済に回すと仮定）。
この推定年間決済額（`estimatedAnnualSpend`）が、以下4パターンすべての判定基準になる。

---

## 3. クエストパターン A〜D（`evaluateCreditCardQuests`）

### パターンA：低効率装備の換装

- **クエストID**: `cardPatternA`
- **発生条件**: メインカード（`creditCards.main`）の `category === 'standard'`
- **節約可能額の計算式**:
  ```
  monthlySaving = floor(floor(推定年間決済額 × 1%) / 12)
  ```
  （目標還元率1.0%＝`CARD_TARGET_RATE_PERCENT`との差分を、還元率0.5%前後の標準カードとの差とみなす概算）
- **メインタイトル**: 【低効率装備の換装】
- **中立表記**: 基本還元率の低いカードから、高還元カードへの乗り換えを促す。
- **詳細テキスト**:
  > 普段使いのメインカードが、還元率0.5%前後の標準的なカードのままかもしれません。年間の決済額次第では、還元率1.0%以上の高還元カードに乗り換えるだけで、同じ買い物でも受け取れるポイントが単純に倍増します。
- **チェック時の動的テキスト**: 🎉 還元率の高いカードに乗り換えた！

### パターンB：呪いの装備の解除（休眠カード）

- **クエストID**: `cardPatternB`
- **発生条件**: 「その他保有カード」（`creditCards.others`）のうち、年会費が発生する（`annualFee > 0`）カードが1枚以上
- **節約可能額の計算式**:
  ```
  totalFee = 該当カードの年会費合計
  monthlySaving = floor(totalFee / 12)
  ```
- **メインタイトル**: 【呪いの装備の解除（休眠カード）】
- **詳細テキスト**:
  > 【クエスト発生】呪いの装備を外してHPの漏れを塞げ！
  > あなたの装備袋から、不穏な気配がします。現在保有している対象カードは、十分な活躍をしていないにもかかわらず、毎年『年会費』というスリップダメージをあなたのHP（手取り）に与え続けています。なんとなく持っているだけの「呪いの装備」は、思い切って手放すのが冒険の鉄則です。今すぐカード会社のマイページや窓口に向かい、装備を解呪（解約・ダウングレード）して、奪われていた手取りを取り戻しましょう！
- **サブタイトル（対象カードと年会費合計を動的表示）**: `対象：{カード名を「・」区切りで列挙}（年会費合計 ¥{合計}）`
- **チェック時の動的テキスト**: 🎉 休眠カードを解約した！

### パターンC：100万円未達の罠

- **クエストID**: `cardPatternC`
- **発生条件**: 保有カード（メイン・サブ・その他のいずれか）に `is1M_Challenge === true` のカードがあり、
  かつ `推定年間決済額 < 1,000,000円`
- **節約可能額の計算式**:
  ```
  monthlySaving = floor((対象カードの年会費 + 推定年間決済額 × 0.5%) / 12)
  ```
- **メインタイトル**: 【100万円未達の罠】
- **中立表記**: 100万円決済に届いていないため、年会費が発生し還元率も0.5%に留まっている状態を警告。
- **詳細テキスト**:
  > 「年間100万円決済で年会費無料・還元率アップ」という強力な特典を持つカードを保有していますが、現在の推定決済額では100万円のラインに届いていません。届かない場合は年会費というコストだけが残ってしまいます。決済を集約するか、届きそうにない場合は保有の見直しを検討しましょう。
- **チェック時の動的テキスト**: 🎉 カードの使い方を見直した！
- **対象カード（`is1M_Challenge: true`）**: 三井住友カード ゴールド（NL）、エポスゴールドカード

### パターンD：コスト割れ特化装備

- **クエストID**: `cardPatternD`
- **発生条件**: 保有カード（メイン・サブ・その他のいずれか）に `breakEvenAmount` が設定されており、
  `isStatusCard !== true`（聖域カードでない）、かつ `推定年間決済額 < breakEvenAmount`
- **節約可能額の計算式**:
  ```
  monthlySaving = floor(対象カードの年会費 / 12)
  ```
- **メインタイトル**: 【コスト割れ特化装備】
- **中立表記**: 無料宿泊特典などのメリットを享受できる決済ラインに届いておらず、年会費が隠れ負債になっている状態を警告。
- **詳細テキスト**:
  > 無料宿泊特典など、一定の決済額に到達すると強力な特典が得られる特化カードを保有していますが、現在の推定決済額ではその損益分岐点に届いていません。年会費だけが負担になっている可能性があるため、決済の集約や保有の見直しを検討しましょう。
- **チェック時の動的テキスト**: 🎉 カードの使い方を見直した！
- **対象カード（`breakEvenAmount` 設定あり・聖域カード除く）**: ヒルトン・オナーズ アメックス（¥1,500,000）

> 補足：ANA SFCゴールドカードは `breakEvenAmount` が未設定かつ `isStatusCard: true` のため、
> パターンC・Dいずれの判定にもかからない「聖域カード」として扱われる。

---

## 4. 足切り・共通ルール

- 4パターンいずれも `monthlySaving < 500円`（`QUEST_MIN_SAVING`）の場合は一覧に表示されない
- 複数パターンに該当する場合、`buildQuestList()` は該当する全パターンをそれぞれ1件ずつ生成する
  （例：メインが標準カードかつ100万円修行カードを別途保有していれば、A・Cが同時に発生しうる）
